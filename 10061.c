#include <stdio.h>
#include <string.h>

int main(){
    int num;
    int count = 0;
    int j, i;
    int key = 0;
    scanf("%d", &num);
    char str[num];
    scanf("%s", str);
    for(int i = 1; i <= num; i++){
        if(num % i == 0){
            for(int j = 0; j < num / i; j++){
                if()
            }
        }
    }
    return 0;
}